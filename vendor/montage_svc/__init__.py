"""montage-svc — OpenMontage's render/craft capabilities as stateless HTTP endpoints.

Contains zero LLM calls and zero generation-API calls. It only transforms files
it is given (or told to fetch).
"""

__version__ = "0.1.0"
